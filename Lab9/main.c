#define MAX (x, y) \
int result = 0;         \
if (x >= y){            \
    result = x;         \
} else {                \
    result = y;         \
}                       \
return result;          \

int main(void)
{
    return 0;
}